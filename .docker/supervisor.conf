[supervisord]
nodaemon = true
pidfile = /run/supervisord.pid

[program:rr]
command = rr serve
user = root
autostart = true

[program:nginx]
command = /usr/sbin/nginx
user = root
autostart = true

[program:php-fpm]
command = /usr/sbin/php-fpm -F
user = root
autostart = true
